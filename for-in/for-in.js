// Iterate through the properties of an object and print each property.

let obj = {
    name: "Dati",
    age: 18,
    address: "Tsagareli 35"
}

for (const key in obj) {
    console.log(key);
}

console.log('--------------------------');
// Iterate through the indices of an array and print each index.

let arr = [
    "Apple",
    "Boy",
    false,
    "Car"
]

for (let key in arr) {
    console.log(key);
}

console.log('-----------------------');

// Iterate through the values of an object and print each value.



